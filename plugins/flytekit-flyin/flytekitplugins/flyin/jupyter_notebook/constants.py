# The default generated path for the Jupyter Notebook config.
DEFAULT_CONFIG_FILE_PATH = ".jupyter/jupyter_notebook_config.py"

# Default ws ping timeout to terminate the jupyter notebook
HOURS_TO_SECONDS = 60 * 60
WS_PING_TIMEOUT = 10 * HOURS_TO_SECONDS  # 10 hours

# Duration to pause the checking of the heartbeat file until the next one
HEARTBEAT_CHECK_SECONDS = 60
