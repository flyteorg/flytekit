apiVersion: v1
kind: Service
metadata:
  name: lepton-connector-service
  namespace: flyte
spec:
  selector:
    app: lepton-connector
  ports:
  - protocol: TCP
    port: 8000
    targetPort: 8000
  type: ClusterIP
